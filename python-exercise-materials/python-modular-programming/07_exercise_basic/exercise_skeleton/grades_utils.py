"""
EXERCISE 1 (BASIC) SKELETON - Create Your First Module
=============================================================
Split score-checking code into its own module. This is
grades_utils.py - the reusable module: calculation logic only.
main.py (in this same folder) imports and uses it.

Fill in the TODOs below, then run main.py (not this file) to test:
    python main.py
"""

def calc_average(scores):
    # TODO: return the average of `scores`, rounded to 2 decimal places
    # HINT: sum(scores) / len(scores), then round(..., 2)
    pass


def get_grade(average):
    # TODO: return a letter grade based on `average`:
    #   90 and above -> "A"
    #   80 and above -> "B"
    #   70 and above -> "C"
    #   60 and above -> "D"
    #   below 60     -> "E"
    pass
